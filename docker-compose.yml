version: '3.8'

services:
  syncflow:
    build: .
    container_name: syncflow
    restart: always
    ports:
      - "3000:3000"
    volumes:
      - ./uploads:/app/uploads
      - ./data:/app/data
    environment:
      - PORT=3000
      - NODE_ENV=production
